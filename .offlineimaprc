[general]
ui = TTY.TTYUI
accounts = Gmail, School
maxsyncaccounts = 3
fsync = False

[Account Gmail]
localrepository = GmailLocal
remoterepository = GmailRemote
status_backend = sqlite

[Repository GmailLocal]
type = Maildir
localfolders = ~/.maildir/Gmail
nametrans = lambda folder: {'drafts':  '[Gmail]/Drafts',
                       'sent':    '[Gmail]/Sent Mail',
                       'trash':   '[Gmail]/Trash'
                           }.get(folder, folder)

[Repository GmailRemote]
auth_mechanisms = LOGIN
type = Gmail
remoteuser = andy.bill.smith@gmail.com
remotepass = PASS
ssl = yes
sslcacertfile = /usr/local/etc/openssl/cert.pem
maxconnections = 1
realdelete = no
nametrans = lambda folder: {'[Gmail]/Drafts':    'drafts',
                       '[Gmail]/Sent Mail': 'sent',
                       '[Gmail]/Trash':     'trash'
                           }.get(folder, folder)
folderfilter = lambda folder: folder not in ['Yahoo!', '[Gmail]/Spam', '[Gmail]/All Mail', '[Gmail]/Starred', '[Gmail]/Important', '[Readdle]', '[Readdle]/Later']

[Account School]
localrepository = SchoolLocal
remoterepository = SchoolRemote
status_backend = sqlite

[Repository SchoolLocal]
type = Maildir
localfolders = ~/.maildir/School

[Repository SchoolRemote]
type = IMAP
remoteuser = 18smitha@smtexas.org
remotepass = PASS
remotehost = localhost
remoteport = 1143
maxconnections = 1
ssl = no
realdelete = no
